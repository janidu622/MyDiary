name: Keep GitHub Green

on:
  schedule:
    - cron: "0 12 * * *"  # Runs daily at 12:00 PM UTC
  workflow_dispatch:  # Allows manual triggering

jobs:
  keep-green:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Configure Git
      run: |
        git config --local user.email "janussiriwardhana@gmail.com"
        git config --local user.name "janidu622"
        
    - name: Create or update activity file
      run: |
        mkdir -p .github/activity
        echo "Last activity: $(date -u '+%Y-%m-%d %H:%M:%S UTC')" > .github/activity/activity.log
        echo "Commit count: $(($(cat .github/activity/count.txt 2>/dev/null || echo 0) + 1))" > .github/activity/count.txt
        
    - name: Commit changes
      run: |
        git add .github/activity/
        git commit -m "🌱 Keep the grass green - $(date -u '+%Y-%m-%d')" || echo "No changes to commit"
        git push
